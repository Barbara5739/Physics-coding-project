# define class Alien_visitor

#other relevant aspects to consider: orbital eccentricity
#defining relevant functions
#underlying formula: barker's equation

import math
import numpy as np

class Alien_visitor:

    def __init__(self, name, mass, periapsis_distance, orbital_eccentricity, initial_position_x, initial_position_y, initial_velocity_x, initial_velocity_y):
        self.name = name
        self.mass = mass
        self.periapsis_distance = periapsis_distance #logically I am not sure if this works but will leave here as a placeholder
        self.orbital_eccentricity = orbital_eccentricity
        self.initial_position_x = initial_position_x
        self.inital_position_y = initial_position_y
        self.initial_velocity_x = initial_velocity_x
        self.initial_velocity_y = initial_velocity_y
        
        self.time = 0
        
    
    def update_angle_alien(self):
        initial_angle = [2, 1] #update this when we figure out how to read in data
        true_anomaly = np.angle(initial_angle)

    def calculate_initial_distance (self):
        return math.sqrt((x_sun-self.initial_position_x) **2 + (y_sun -self.initial_position_y)** 2)

    def set_initial_velocity(self):
        x = self.initial_velocity_x * math.cos(self.true_anomaly)
        y = self.initial_velocity_y * math.sin(self.true_anomaly)
    
    def alien_position(self):    
        return 2* self.periapsis_distance/(1+ math.cos(self.true_anomaly)) # this returns the radius to relevant star based on an equation I found

    def velocity_at_time(self):
        self.velocity_at_time = velocity_at_time
        velocity_at_time = math.sqrt(2*G*self.mass_sun/self.alien_position) # check this once we have stuff on sun


    def plot_orbit(self):
        pass
